services:
  api:
    build: .
    ports:
      - "${HTTP_PORT:-8080}:8080"
    environment:
      MONGODB_URI: ${MONGODB_URI}
      MONGODB_DATABASE: ${MONGODB_DATABASE:-restapi}
      MONGODB_COLLECTION_ITEMS: ${MONGODB_COLLECTION_ITEMS:-items}
      HTTP_ADDR: :8080
